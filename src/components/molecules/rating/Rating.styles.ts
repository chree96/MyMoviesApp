import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontSize: 18,
    fontWeight: 'bold',
  },
  rowContainer: {
    flexDirection: 'row',
    paddingTop: 4,
  },
  voteContainer: {
    justifyContent: 'center',
    paddingLeft: 8,
  },
  description: {
    fontSize: 16,
  },
  bigTitle: {
    fontWeight: 'bold',
    fontSize: 24,
  },
});
